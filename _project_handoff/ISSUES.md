# 当前未解决问题

## 1. 命令行参数还不够完整

- 现象：当前多个脚本主要只能通过命令行传入 `--target_x` 和 `--target_y`。
- 可能原因：第一阶段优先保证最小 demo 可运行，尚未扩展更多实验参数。
- 已尝试方法：已在代码中保留 `run_reach_simulation` 的默认参数，包括连杆长度、初始角度、最大迭代次数和误差阈值。
- 下一步建议：在 demo 和实验脚本中增加连杆长度、初始姿态、迭代次数、阈值和参数列表等可选参数，并补充 README 示例。

## 2. 测试覆盖场景较少

- 现象：当前测试已经覆盖基础运动学、解析逆运动学、目标点收敛、阻尼控制和通用仿真历史，但实验脚本本身还没有端到端测试。
- 可能原因：项目仍处于第一版展示阶段，测试重点是验证主流程能跑通。
- 已尝试方法：已运行 `pytest`，当前结果为 `12 passed`。
- 下一步建议：增加多目标点、多初始姿态、边界目标点、奇异姿态附近测试，以及实验脚本 smoke test。

## 3. `configs/` 目录当前为空

- 现象：项目根目录有 `configs/` 目录，但没有配置文件。
- 可能原因：后续可能计划加入配置管理，但当前功能还没有用到。
- 已尝试方法：已扫描目录，确认当前为空。
- 下一步建议：待确认是否保留该目录；如果保留，可添加默认参数配置；如果短期不用，可考虑删除空目录。

## 4. 解析逆运动学目前只做文本对比

- 现象：`scripts/run_compare_methods.py` 会打印解析逆运动学和雅可比伪逆结果，但还没有画出 elbow up/down 两种机械臂姿态。
- 可能原因：第二阶段先补充算法和数值对比，视觉化还没有展开。
- 已尝试方法：已新增工作空间图，帮助理解可达范围。
- 下一步建议：增加两种解析解姿态图，或在同一张图上叠加 elbow up/down 构型。

## 5. 运行时图片和展示图片分离后需要维护

- 现象：demo 生成图片到 `outputs/`，README 展示图片来自 `docs/assets/`。
- 可能原因：为了避免每次运行覆盖 GitHub README 展示图，采用了运行输出和展示资产分离的方案。
- 已尝试方法：已把当前三张展示图片复制到 `docs/assets/`；第二阶段工作空间图仍作为运行输出，不提交生成图片。
- 下一步建议：当 demo 视觉效果发生明显变化时，重新生成图片并同步更新 `docs/assets/`。

## 6. 远端推送状态待确认

- 现象：本地仓库已连接 `origin`，但当前交接文档新增后还没有提交和推送。
- 可能原因：用户要求不要自动 push。
- 已尝试方法：已检查本地 Git 状态和远端配置。
- 下一步建议：用户确认后执行 `git add .`、`git commit`，再决定是否 `git push`。

## 7. 第三阶段实验结论还没有正式报告

- 现象：已经生成参数扫描 CSV 和多张曲线图，但还没有把实验现象整理成完整报告。
- 可能原因：本阶段先搭建实验脚本和输出流程。
- 已尝试方法：已新增 `docs/07_experiment_report_template.md` 作为报告模板。
- 下一步建议：基于 `outputs/logs/parameter_sweep.csv` 和 `outputs/figures/` 曲线图填写实验报告。
