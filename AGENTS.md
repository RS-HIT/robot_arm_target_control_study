# AGENTS.md

本文件是 Codex 以后进入 `robot_arm_target_control_study` 项目时应优先阅读的长期工作规则。它不是阶段汇报，也不是项目 README 的替代品。

## 1. 项目定位

`robot_arm_target_control_study` 是一个面向机器人算法入门与展示的 2D 平面二连杆机械臂控制实验项目，不是复杂仿真平台。

项目核心目标是：能运行、能改参数、能看懂数据流、能解释算法、能写进 README/GitHub/简历。当前不追求大型工程化系统，不主动引入 MuJoCo、ROS、强化学习、复杂动力学或三维机械臂。

## 2. 用户背景

用户是机械/机器人方向研究生，非计算机科班。简单 Python 可以阅读，但大型复杂代码结构会增加理解成本。

后续代码应尽量简单、清晰、注释充分。解释算法时要重视输入、输出、数据流、公式含义和实验现象。

## 3. 工作方式偏好

- 小步推进，每次只完成一个清晰目标。
- 先说明计划，再改代码。
- 不一次性堆大量新功能。
- 修改后给出运行命令和验收标准。
- 保持项目可运行、可测试、可解释、可展示。
- 如果发现需求过大，优先拆成能独立验证的小任务。

## 4. 目录结构约定

- `README.md`：项目主说明，记录阶段目标、运行命令和输出说明。
- `scripts/`：实验入口脚本，用户主要从这里运行实验。
- `src/robot_arm_target_control_study/`：核心算法代码。
- `tests/`：pytest 测试。
- `docs/`：学习笔记、阶段文档、实验说明和报告模板。
- `outputs/figures/`：图像输出。
- `outputs/logs/`：CSV 和实验日志。
- `_project_handoff/`：历史交接资料。

## 5. 代码修改原则

- 不要大规模重构。
- 不要引入复杂类继承。
- 不要为了“高级”而复杂化。
- Python 文件尽量保持短小清晰。
- 新增函数必须有中文 docstring，说明作用、输入、输出。
- 关键公式旁边要有中文注释，帮助非计算机科班用户理解。
- 运动学、控制器、轨迹生成、仿真、绘图、指标计算尽量放在各自模块中。
- 修改已有功能时，要避免破坏已稳定的脚本和测试。

## 6. 测试与运行要求

每次修改后至少考虑运行：

```bash
pytest
```

并运行与本次修改相关的 `scripts/run_*.py`。

如果新增脚本，应给出示例运行命令。如果新增指标、CSV 或图像输出，应确认输出文件路径存在。如果无法运行，应在总结中明确说明原因。

## 7. 文档更新要求

- 新增功能时同步更新 `README.md`。
- 新增学习内容时同步更新 `docs/`。
- 重要实验要说明运行命令、输出文件、如何读图。
- 不要只写“实现了功能”，还要写“这个功能帮助理解什么概念”。
- 文档语言应适合非计算机科班用户理解。

## 8. 实验记录要求

- 参数实验、轨迹实验和对比实验应尽量输出 CSV。
- 图像输出保存到 `outputs/figures/`。
- 日志和表格保存到 `outputs/logs/`。
- 实验结论不要只看 `final_error`，也要看误差曲线、关节角曲线、速度/加速度曲线和约束 violation。
- 如果实验输出会覆盖旧文件，应提醒用户，必要时建议增加 `tag` 或独立输出目录。

## 9. 禁止事项

- 不要擅自引入 MuJoCo、ROS、PyBullet、强化学习、复杂动力学或三维机械臂。
- 不要把项目重构成大型框架。
- 不要删除已有学习文档和已有稳定脚本。
- 不要用英文长篇注释替代中文解释。
- 不要生成大量用户难以阅读的复杂代码。
- 不要只给代码而不解释数据流。
- 不要把 `AGENTS.md` 写成阶段进展汇报。

## 10. 每次任务结束前的检查清单

每次完成任务后，Codex 应总结：

- 修改了哪些文件；
- 新增了哪些函数或脚本；
- 如何运行；
- 如何验证；
- 是否更新 `README.md` 或 `docs/`；
- 是否新增或更新测试；
- 是否生成图像或 CSV；
- 哪些地方需要用户重点阅读；
- 当前是否还有未解决问题。

